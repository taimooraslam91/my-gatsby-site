import * as React from "react";
import { Link } from "gatsby";

// markup
const NotFoundPage = () => {
  return (
    <main>
      <h2>Not Found</h2>
    </main>
  );
};

export default NotFoundPage;
